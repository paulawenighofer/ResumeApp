<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:ResumeApp.Views.Controls"
             x:Class="ResumeApp.Views.Controls.PageHeader"
             x:DataType="controls:PageHeader"
             x:Name="Root">

    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" Padding="4,4">
        <Border WidthRequest="36"
                HeightRequest="36"
                BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceWhite}, Dark={StaticResource Gray900}}"
                Stroke="{AppThemeBinding Light={StaticResource CardBorder}, Dark={StaticResource InputStrokeDark}}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 12">
            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnBackTapped" />
            </Border.GestureRecognizers>
            <Label Text="&#xf053;"
                   FontFamily="FASolid"
                   FontSize="14"
                   TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource White}}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Border>
        <Label Grid.Column="1"
               Text="{Binding Title, Source={x:Reference Root}}"
               FontFamily="OpenSansSemibold"
               FontSize="18"
               TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource White}}"
               VerticalOptions="Center" />
    </Grid>
</ContentView>
