apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: resumeapp
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s
    scrape_configs:
      - job_name: 'api'
        static_configs:
          - targets: ['resumeapp-api-service:80']