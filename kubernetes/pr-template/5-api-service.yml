apiVersion: v1
kind: Service
metadata:
  name: resumeapp-api-service
  namespace: $PR_NAMESPACE
spec:
  type: ClusterIP
  selector:
    app: resumeapp-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
